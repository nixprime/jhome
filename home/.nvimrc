set runtimepath+=~/.vim

" Assume we're on a terminal supporting 24-bit color (e.g. xterm)
let $NVIM_TUI_ENABLE_TRUE_COLOR=1

source ~/.vimrc

" <Leader><Enter> opens a terminal
nmap <Leader><CR> :terminal<CR>
